<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Budget.MainPage"
             Title="Sākums">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,60"
            Spacing="25"
            VerticalOptions="Center">

            <Label
                Text="Čau!"
                FontSize="32"
                HorizontalOptions="Center" />

            <Label
                Text="Šī ir tava aplikācija budžetam"
                FontSize="20"
                HorizontalOptions="Center" />

            <Button
                Text="Ieiet"
                Clicked="OnEnterClicked"
                HorizontalOptions="Center"
                BackgroundColor="#007AFF"
                TextColor="White"
                CornerRadius="10"
                Padding="10,5" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
